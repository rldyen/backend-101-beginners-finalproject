<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cafe.cafeName %></title>
    <link rel="stylesheet" href="../styles/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="bg-gray-800 container py-10 px-10 mx-0 min-w-full flex flex-row justify-center ">
      <form action="/cafe/<%= cafe._id %>/updateCafe">
        <input type="submit" class="text-xl mx-2 text-white content-center bg-blue-500 hover:bg-blue-700 font-bold py-2 px-4 rounded-lg hover:cursor-pointer" value="Update" />
      </form>
      <form action="/cafe/<%= cafe.id %>?_method=DELETE" method="POST">
        <input type="submit" class="text-xl mx-2 text-white content-center bg-red-700 hover:bg-red-800 font-bold py-2 px-4 rounded-lg hover:cursor-pointer" value="Delete" />
      </form>
    </div>
    <div class="min-h-screen bg-gray-800">
        <div class="pt-1"></div>
        <div class="rounded overflow-hidden shadow-lg bg-white w-[50%] m-auto">
            <div class="px-6 py-4">
              <div class="font-bold text-3xl mb-2"><%= cafe.cafeName %></div>
              <p class="text-gray-700 text-xl mt-3">
                <strong>Author:</strong> <%= cafe.uploader %>
              </p>
              <p class="text-gray-700 text-xl mt-3">
                <strong>Rating:</strong> <%= cafe.rating %>/5
              </p>
              <p class="text-gray-700 text-xl mt-3 text-justify">
                <strong>Description:</strong> <%= cafe.description %>
              </p>
              <p class="text-gray-700 text-xl mt-3">
                <strong>Address:</strong> <%= cafe.cafeAddress %>
              </p>
            </div>
            <div class="content-center">
                <iframe src="https://www.google.com/maps?q=<%= cafe.cafeAddress %>&output=embed" width="950" height="250" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade" ></iframe>
            </div>
          </div>
          <div class="px-6 pt-4 pb-2 flex flex-col">
            <a 
                class="inline-block px-3 py-1 text-xl font-semibold text-white mr-2 mb-2 hover:text-gray-200 text-center" 
                href="/home">Go Back To Home</a>
          </div>
    </div>

    <%- include('../partials/footer') %>

</body>
</html>