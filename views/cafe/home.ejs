<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <link rel="stylesheet" href="./styles/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="min-h-screen bg-gray-800">
        <div class="h-24">

            <form action="/cafe/add">
                <input type="submit" class="text-xl float-right mt-8 text-white content-center bg-green-500 hover:bg-green-700 font-bold py-2 px-6 mr-12 rounded-lg hover:cursor-pointer" value="Add" />
            </form>
        </div>
        
        <div class="w-full">
            <h1 class="text-center text-white font-bold text-3xl">List of Café</h1>
        </div>

        <div class="p-10 grid grid-cols-4 sm:grid-cols-2 md:grid-cols-4 lg:grid-cols-4 xl:grid-cols-4 gap-5">
            <% for (let l of listings){ %>
            <!--Card-->
            <div class="flex flex-col rounded overflow-hidden shadow-lg bg-white">
              <div class="px-6 py-4">
                <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2 mb-2"><%= l.uploader %></span>
                <div class="font-bold text-3xl mb-4"><%= l.cafeName %></div>
                <p class="text-gray-700 text-base truncate text-ellipsis overflow-hidden mb-2">
                    <%= l.description %>
                </p>
                <p class="text-gray-700 text-base mb-2">
                    Located at <%= l.cafeAddress %>
                </p>
              </div>
              <div class="px-6 pt-4 pb-2 mt-auto">
                <a 
                    class="inline-block bg-blue-600 rounded-sm px-3 py-1 text-lg font-semibold text-white mr-2 mb-2 hover:bg-blue-800 hover:text-gray-200 w-full text-center" 
                    href="/cafe/<%= l._id %>">See more details</a>
              </div>
            </div>
            <% } %>
          </div>
        </div>
    <%- include('../partials/footer') %>
    
</body>
</html>